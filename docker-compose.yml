version: "3.7"

services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: $PGUSER
      POSTGRES_PASSWORD: $PGPASSWORD
      POSTGRES_DB: $PGDATABASE
    ports:
      - "5432:5432"
    volumes:
      - ./:/app
    working_dir: /app
